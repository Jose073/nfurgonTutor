<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/nfurgonlogolauncher"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="16dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:gravity="center_horizontal"
        android:padding="16dp">
        <Button
            android:layout_width="match_parent"
            style="@style/CustomButton"
            android:layout_height="wrap_content"
            android:id="@+id/btn_phone_singin"
            android:padding="16dp"
            android:layout_marginBottom="10dp"
            android:drawableStart="@drawable/baseline_phone_24"
            android:text="@string/inicia_sesi_n_con_tel_fono"
            android:textColor="@android:color/black"
            />
        <Button
            android:layout_width="match_parent"
            style="@style/CustomButton"
            android:layout_height="wrap_content"
            android:id="@+id/btn_google_singin"
            android:padding="16dp"
            android:layout_marginBottom="10dp"
            android:drawableStart="@drawable/fui_ic_googleg_color_24dp"
            android:text="@string/inicia_sesi_n_con_google"
            android:textColor="@android:color/black"
            />
    </LinearLayout>
</RelativeLayout>